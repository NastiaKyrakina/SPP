app-viewport
    div(main="")
        .feed.row
            .col.aside-menu
                .menu
                    button.catg-btn(*ngFor = 'let ctg of ctgList',
                    (click) = 'selectCategory(ctg)',
                    appQueryParamsActive
                    className = 'selected',
                    paramName='ctg',
                    [paramValue]='ctg') {{ctg}}
            .col.main-content
                div(*ngIf='workshops$ | async as workshops; else empty' )
                    app-article(*ngFor='let workshop of workshops',
                    [workshop] = 'workshop',
                    [tags] = 'tags | workshopTags: workshop.tags')

                button.secondary((click) = 'loadArticles()',
                [hidden]='emptyPage') Load more articles...
            .col.tags
                .tag-cloud
                    ng-container(*ngIf='tagsLoaded; else emptyTags')
                        app-tag(*ngFor='let tag of tags',
                        [tag] = 'tag',
                        (selectTag)='selectTag($event)',
                        appTextToColor
                        [text]='tag.name',
                        [hoverEffect] = '"on"',
                        [effectType] = '"darken"' ,
                        appQueryParamsActive
                        paramName="tags",
                        [paramValue]='tag.seq'
                        )
                    ng-template(#emptyTags='')
                        div.tags-loaded Tags Loaded....
