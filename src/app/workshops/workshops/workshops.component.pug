app-viewport
    div(main="")
       .feed.row(*ngIf = 'users$ | async as users')
            .col.aside-menu
                .menu
                    button.catg-btn(*ngFor = 'let ctg of ctgList',
                    (click) = 'selectCategory(ctg)',
                    appQueryParamsActive
                    className = 'selected',
                    paramName='ctg',
                    [paramValue]='ctg') {{ctg}}
                .menu-control
                    a.control-btn(
                    routerLink='/workshops/create'
                    ) Add workshop
            .col.main-content
                app-article('*ngFor'='let workshop of workshops$ | async',
                [workshop] = 'workshop',
                [user] = 'users|author:workshop.author',
                [tags] = 'tags | workshopTags: workshop.tags',
                (workshopDeleted) = 'deleteWorkshop($event)')
                button.secondary(
                    (click) = 'loadArticles()',
                    [hidden]='emptyPage') Load more articles...
            .col.tags
                .tag-cloud
                    ng-container(*ngIf='tagsLoaded; else emptyTags')
                        app-tag(*ngFor='let tag of tags',
                        [tag] = 'tag',
                        (selectTag)='selectTag($event)',
                        appTextToColor
                        [text]='tag.name',
                        [hoverEffect] = '"on"',
                        [effectType] = '"darken"' ,
                        appQueryParamsActive
                        paramName="tags",
                        [paramValue]='tag.seq'
                        )
                    ng-template(#emptyTags='')
                        div.tags-loaded Tags Loaded....
