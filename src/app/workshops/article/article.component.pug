app-card(*appCollapsable = 'let state set "exp" height "100px" animation "on"; let ctrl = controller')
    .card-header(card-header='')
        a([routerLink]='["../workshop", workshop.id]')
            figure
                img([src]='workshop.img')
                figcaption {{workshop.title}}
    .desc-block
         p
            | {{desc + '...'}}
            a([routerLink]='["../workshop", workshop.id]') read more
    .tags.row
        .tag(*ngFor="let tag of tags", [id]='"tag-"+tag.id').
            {{tag.key}}
    .card-footer.row(card-footer='')
        app-like([likes]='workshop.likes', [isActive] = 'likeIt',(likedWorkshop)='liked($event)')
        app-timestamp([timestamp]='workshop.date', [onlyDate]='true')
    .row(card-buttons='')
        button.open-btn(
            (click)='ctrl.expand()', [hidden]='state!="collapsed"') Show More
        button.close-btn(
            (click)='ctrl.collapse()', [hidden]='state=="collapsed"') Show Less
