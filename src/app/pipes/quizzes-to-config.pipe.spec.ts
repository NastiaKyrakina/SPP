import { QuizzesToConfigPipe } from './quizzes-to-config.pipe';

describe('QuizzesToConfigPipe', () => {
  it('create an instance', () => {
    const pipe = new QuizzesToConfigPipe();
    expect(pipe).toBeTruthy();
  });
});
