h3.title {{data?.title}}
p.text {{data?.text}}
.buttons.row.flex-between
    button.susses(
        *ngIf = 'data?.confirmButton',
        (click)='confirm()') {{data?.confirmButton}}
    button.cancel(
        *ngIf = 'data?.cancelButton',
        (click)='cancel()') {{data?.cancelButton}}
