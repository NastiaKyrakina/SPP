import { WorkshopQuizPipe } from './workshop-quiz.pipe';

describe('WorkshopQuizPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkshopQuizPipe();
    expect(pipe).toBeTruthy();
  });
});
