import { WorkshopTagsPipe } from './workshop-tags.pipe';

describe('WorkshopTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkshopTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
